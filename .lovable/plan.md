

O utilizador diz que o relatório público (`/pagamento/:token`) não está a considerar os campos UF_CRM_EMMELY do Bitrix24 (número de parcelas, vencimento, etc.) — está sempre a cair no fallback sintético de 1 parcela.

Preciso ver quais campos UF_CRM_EMMELY existem no Bitrix24 para parcelas/vencimento e como o `payment-receipt` deveria lê-los.
